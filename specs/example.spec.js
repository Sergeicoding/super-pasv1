import { expect } from "chai"

describe( 'Math functions', function() {
    it( 'A + B + C', function () {
      const a = 4
      const b = 5
      const c = a + b
      expect(c).to.eq( 9)
    })

    it('A - B', function () {

    })
})
